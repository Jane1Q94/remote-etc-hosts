name: auto format lint test publish
on:
  - push
jobs:
  format:
    name: format code
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: hello world
        run: echo "hello world!"
